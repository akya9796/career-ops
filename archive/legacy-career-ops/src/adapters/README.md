# Adapter Layer

The existing scanner provider plugins live in `providers/*.mjs`.
This directory is the target home for the next-generation adapter registry.

Migration rule: wrap existing providers first, then add new sources.

