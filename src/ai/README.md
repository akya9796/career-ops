# AI Provider Layer

Provider selection is configured in `config/ai.yml`.

Target providers:

- OpenAI
- Claude
- Gemini
- local Ollama models

All generation modules must return traceability metadata.

