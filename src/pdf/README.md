# PDF Module

PDF generation is provided by the existing root `generate-pdf.mjs` renderer.
Application-assistant modules generate Markdown first; PDF rendering can be
added as a wrapper without changing the factual source-of-truth boundary.

